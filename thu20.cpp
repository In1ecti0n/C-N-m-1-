#include<stdio.h>
#include<string.h>
int ktra (int s) {
    

}
int main () {
    int n, s;
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &s);
    }
    for (int i = 1; i < s; i++) {

    }
}